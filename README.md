# Оксана Терпенёва — Digital Systems Architect

Минималистичная одностраничная визитка с переключателем тёмной/светлой темы.
Построена на **TanStack Start + React 19 + Vite 7 + Tailwind CSS v4**.

---

## 📋 Содержание

1. [Быстрый просмотр (превью)](#-быстрый-просмотр-превью)
2. [Публикация сайта (раскрытие в интернет)](#-публикация-сайта-раскрытие-в-интернет)
3. [Подключение собственного домена](#-подключение-собственного-домена)
4. [Поделиться превью без публикации](#-поделиться-превью-без-публикации)
5. [Локальный запуск](#-локальный-запуск)
6. [Как назвать и где разместить README](#-как-назвать-и-где-разместить-readme)
7. [Структура проекта](#-структура-проекта)
8. [Обновление контента](#-обновление-контента)
9. [Часто задаваемые вопросы](#-часто-задаваемые-вопросы)

---

## 👀 Быстрый просмотр (превью)

Превью обновляется автоматически после каждого изменения в редакторе Lovable.

- **Preview URL:** https://id-preview--c8bf60b4-369b-47a4-92b7-a5ed1279ae7a.lovable.app
- Превью доступно только тем, у кого есть доступ к проекту в Lovable.

---

## 🚀 Публикация сайта (раскрытие в интернет)

Чтобы сайт стал доступен **всем по публичной ссылке**, его нужно опубликовать.

### Шаг 1. Откройте диалог публикации
- **На десктопе:** кнопка **Publish** в правом верхнем углу редактора.
- **На мобильном:** кнопка в правом нижнем углу в режиме Preview.

### Шаг 2. Нажмите «Publish» (первая публикация)
После первой публикации сайт получит постоянный адрес вида:

> **https://ai-automation-glue.lovable.app**

(это уже текущий публичный адрес проекта)

### Шаг 3. Обновление сайта
После любых изменений во **фронтенде** (вёрстка, стили, тексты) нужно
снова открыть диалог Publish и нажать **Update** — изменения станут
доступны на публичной ссылке.

> ⚠️ Изменения в **бэкенде** (edge-функции, миграции БД) деплоятся
> автоматически и не требуют ручного обновления.

### Шаг 4. Проверка видимости
Убедитесь, что в настройках публикации выбрано **Public** —
тогда ссылку сможет открыть любой человек без входа в Lovable.

---

## 🌐 Подключение собственного домена

Перед подключением домена сайт **должен быть опубликован**.

1. Перейдите в **Project Settings → Domains**
   *(или в диалоге Publish → Add custom domain)*.
2. Введите ваш домен (например, `terpeneva.ru`).
3. Добавьте указанные DNS-записи у регистратора домена
   (обычно `A` и/или `CNAME`).
4. Дождитесь проверки DNS и автоматической выдачи SSL-сертификата
   (обычно от нескольких минут до часа).

После этого сайт будет открываться по вашему домену с HTTPS.

📖 Подробнее: https://docs.lovable.dev

---

## 🔗 Поделиться превью без публикации

Если нужно показать сайт **до** официальной публикации:

1. Нажмите **Share → Share preview** в редакторе.
2. Скопируйте созданную ссылку.
3. Ссылка работает **7 дней** и доступна без аккаунта Lovable.

> Это режим «только просмотр» — отличается от публикации тем, что
> ссылка временная.

---

## 💻 Локальный запуск

Если вы выгрузили код из Lovable (через GitHub) и хотите запустить
проект у себя:

```bash
# Установка зависимостей (рекомендуется bun)
bun install

# Запуск dev-сервера
bun run dev

# Сборка production-версии
bun run build
```

После `bun run dev` сайт будет доступен на `http://localhost:5173`.

**Требования:**
- Node.js 20+ или Bun 1.1+
- Современный браузер

---

## 📝 Как назвать и где разместить README

Чтобы файл автоматически отображался на главной странице репозитория
(GitHub, GitLab, Bitbucket, Gitea и др.), важны **имя**, **расширение**
и **расположение**.

### ✅ Правильное имя файла

- **`README.md`** — стандарт. Имя пишется **заглавными буквами**,
  расширение — `.md` (Markdown).
- Регистр имеет значение на Linux-серверах (где работает GitHub):
  `readme.md`, `Readme.md`, `ReadMe.MD` могут **не распознаться** как
  главный файл репозитория.
- Расширение `.md` обязательно — иначе GitHub покажет файл как простой
  текст без форматирования (заголовков, списков, ссылок).

| Имя файла       | Распознаётся GitHub? | Комментарий                  |
|-----------------|----------------------|------------------------------|
| `README.md`     | ✅ Да (рекомендуется) | Стандарт де-факто            |
| `README.MD`     | ✅ Да                 | Работает, но непринято       |
| `Readme.md`     | ⚠️ Иногда             | Лучше не использовать        |
| `readme.md`     | ⚠️ Иногда             | Может не отобразиться        |
| `README.txt`    | ❌ Нет форматирования | Покажется как plain text     |
| `readmy.md`     | ❌ Опечатка           | GitHub не распознает         |
| `README`        | ✅ Да, без разметки   | Работает, но без Markdown    |

### 📂 Где разместить файл

Файл `README.md` должен лежать **в корне репозитория** — рядом с
`package.json`, `vite.config.ts`, `.gitignore`:

```
my-project/
├── README.md          ← здесь, в корне
├── package.json
├── vite.config.ts
├── src/
│   └── ...
└── public/
```

GitHub автоматически найдёт его и отрендерит под списком файлов на
главной странице репозитория.

### 📁 README в подпапках (бонус)

GitHub также показывает `README.md`, лежащий **внутри любой папки** —
он отображается при открытии этой папки. Это удобно для документации
отдельных модулей:

```
src/
├── components/
│   └── README.md    ← покажется при открытии папки components
└── routes/
    └── README.md    ← покажется при открытии папки routes
```

### 🔄 Как загрузить README в репозиторий

1. Подключите проект к GitHub: **Connectors → GitHub → Connect project**.
2. Файл `README.md` автоматически синхронизируется в ваш репозиторий.
3. Откройте репозиторий на github.com — README появится на главной.

### 💡 Полезные советы

- Используйте **Markdown-разметку**: `#` заголовки, `**жирный**`,
  списки, ссылки `[текст](url)`, код в тройных бэктиках.
- Добавляйте **значки (badges)** в начало — версия, лицензия, статус сборки.
- Первый заголовок (`# Название`) GitHub использует как заголовок проекта.
- Поддерживаются эмодзи 🎉 и таблицы для наглядности.

---

## 📁 Структура проекта

```
src/
├── routes/
│   ├── __root.tsx       # Корневой layout, шрифты, тема
│   └── index.tsx        # Главная страница (визитка)
├── components/ui/       # shadcn/ui компоненты
├── styles.css           # Дизайн-токены, фактура фона, темы
└── router.tsx           # Конфигурация роутера
```

---

## ✏️ Обновление контента

### Контактные данные
Файл: `src/routes/index.tsx` — блок с `ContactLink`:

```tsx
<ContactLink href="mailto:ivamarmaris@yandex.ru" icon={Mail} label="email" />
<ContactLink href="https://t.me/ivamarmaris" icon={Send} label="telegram" />
<ContactLink href="https://max.ru/u/..." icon={MessageCircle} label="max" />
```

### Имя, должность, слоган
Там же, в `src/routes/index.tsx` — секция `<section>` с `<h1>` и `<p>`.

### Цвета и тема
Файл: `src/styles.css` — переменные `--background`, `--foreground`,
`--primary`, `--glow` и др. в блоках `:root` и `.dark`.

### SEO (title, description, OG-теги)
Файл: `src/routes/index.tsx` — объект `head()` в `Route`.

---

## ❓ Часто задаваемые вопросы

**Сайт не обновляется после правок — что делать?**
Откройте диалог Publish и нажмите **Update**. Фронтенд требует
ручного обновления публикации.

**Можно ли убрать бейдж «Built with Lovable»?**
Да, в настройках публикации (доступно на платных тарифах).

**Как экспортировать код?**
В Lovable → GitHub → подключите репозиторий и синхронизируйте.

**Куда писать, если что-то сломалось?**
- Документация: https://docs.lovable.dev
- Discord-сообщество: https://discord.gg/lovable

---

© 2026 Оксана Терпенёва. Все права защищены.
